[project]
name = "autonomous-task-agent-backend"
version = "0.1.0"
description = "Autonomous Multi-Tool AI Task Agent backend"
requires-python = ">=3.11"

[project.optional-dependencies]
playwright = ["playwright"]

[tool.uvicorn]
app = "backend.app:app"
host = "0.0.0.0"
port = 8000
reload = true
