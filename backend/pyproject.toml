[project]
name = "autonomous-task-agent-backend"
version = "2.0.0"
description = "Autonomous multi-agent AI research assistant"
requires-python = ">=3.11"

[tool.uvicorn]
app = "backend.app:app"
host = "0.0.0.0"
port = 8000
